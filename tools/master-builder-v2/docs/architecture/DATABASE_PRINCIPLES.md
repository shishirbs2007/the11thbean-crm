# Database Principles

- UUID primary keys
- Soft delete by default
- Immutable timeline
- RLS on every table
- Audit every mutation
- Migration-only schema changes
- JSONB only where appropriate
- Foreign keys everywhere possible
- Index every lookup path
- No business logic in the UI
