export default function CustomersPage() {
  return (
    <main className="mx-auto min-h-screen max-w-5xl px-6 py-16">
      <p className="text-sm font-semibold uppercase tracking-[0.2em]">
        Staff workspace
      </p>
      <h1 className="mt-3 text-4xl font-semibold tracking-tight">Customers</h1>
      <div className="mt-10 rounded-2xl border p-6">
        <h2 className="text-lg font-semibold">Foundation ready</h2>
        <p className="mt-2 max-w-2xl leading-7 text-neutral-600">
          The database, security policies and integration ledger are prepared.
          Customer search, profile views and POS field mapping are the next
          application layer.
        </p>
      </div>
    </main>
  );
}
